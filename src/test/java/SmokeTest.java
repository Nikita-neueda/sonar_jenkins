import static org.junit.jupiter.api.Assertions.assertTrue;
import org.junit.jupiter.api.Test;

public class SmokeTest {
    @Test
    void pipelineCheck() {
        // Simple assertion that always passes to satisfy the Jenkins test reporter
        assertTrue(true);
    }
}
